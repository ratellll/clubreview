spring:
  profiles:
    active: dev # 기본 프로파일 설정

---

spring:
  config:
    activate:
      on-profile: dev # 개발 모드 (H2 사용)
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

---

spring:
  config:
    activate:
      on-profile: prod # 운영 모드 (AWS RDS 사용)
  datasource:
    url: jdbc:mysql://clubreview-db.cvqec6guwwhq.us-east-1.rds.amazonaws.com:3306/clubreview-db
    driverClassName: com.mysql.cj.jdbc.Driver
    username: admin
    password: homertla1!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

---

spring:
  config:
    activate:
      on-profile: local # 로컬 MySQL 모드
  datasource:
    url: jdbc:mysql://localhost:3306/db_clubreview
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#-Dspring.profiles.active=local
#-Dspring.profiles.active=prod