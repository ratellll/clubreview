spring:
  profiles:
    active: prod # 기본 프로파일 설정

---

#spring:
#  config:
#    activate:
#      on-profile: dev # 개발 모드 (H2 사용)
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driverClassName: org.h2.Driver
#    username: sa
#    password:
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

---

spring:
  config:
    activate:
      on-profile: prod # 운영 모드
  datasource:
    url: jdbc:mysql://localhost:3306/db_clubreview
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: homertla1!Q
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  thymeleaf:
    prefix: classpath:/templates/ # 템플릿 기본 경로
    suffix: .html                 # 템플릿 파일 확장자
    cache: false                  # 개발 시 캐시 비활성화

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE



---

spring:
  config:
    activate:
      on-profile: local # 로컬 MySQL 모드
  datasource:
    url: jdbc:mysql://localhost:3306/db_clubreview
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#-Dspring.profiles.active=local
#-Dspring.profiles.active=prod